// the main idea is a opposite of stack
// stack has to push thing at the top and take things at the top, which means first in last out
// queue has to push and pop at different side, which means first in first out like in line

#include <stdio.h>
#include <stdlib.h>

typedef struct {
    int arr[5];
    int top;
    int bottom;
};

void push(int val) {
    
}